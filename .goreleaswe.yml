project_name: rush

env:
  - GO111MODULE=on

builds:
  - env:
      - CGO_ENABLED=1
    id: rush-linux-windows
    ldflags:
      - -s -w
    main: ./main.go
    binary: hellod
    goos:
      # - darwin
      - linux
    goarch:
      - amd64
      - windows
    tags:
      - release

archives:
  -
    name_template: "{{.Os}}-{{.Arch}}{{if .Arm}}v{{.Arm}}{{end}}-{{ .ProjectName }}"
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip